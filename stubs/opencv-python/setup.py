from setuptools import setup

setup(name='opencv-python',
      version='4.6.0.66',
      description='OpenCV Stub for already compiled version',
      packages=['opencv-python'],
      zip_safe=False)
